import transactionTypes from "./transaction_types";
import frequencies from "./frequencies";

// Transaction Type: {id: 0, name: "Whatever", amount: 324.33, frequency:"daily" }

export function forecastBudget(transactions) {

}